{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "docker:up": "docker compose --project-directory ./ --env-file .env -f db/docker-compose.yml up"
  }
}